/ 1: rename z() to $() call anchorTo anchorTo()

/2: replace parseInt|Float() with parseFloat(..., 10)
/3: if I bind a box to the right of something and then resize it, it should still stick to the right
        > visual spec
        > spec
/4: if I bind to the right of an ancestor I don't want it's right position, I want width (similar with bottom/height)
        > visual spec
        > spec

/5: stop dragging on window lost focus
/6: checkout fractional layouts


/ next: Supermodel
  
